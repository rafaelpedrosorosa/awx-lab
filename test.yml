---
- name: Teste AWX
  hosts: all
  gather_facts: false

  tasks:
    - name: Ping
      ansible.builtin.ping:

    - name: Criar arquivo
      ansible.builtin.copy:
        dest: /tmp/awx-ok.txt
        content: "AWX funcionando\n"
